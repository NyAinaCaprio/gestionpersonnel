{% extends 'base.html.twig' %}

{% block title %}EtsouService{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>EtsouService</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etsou_service.id }}</td>
            </tr>
            <tr>
                <th>Etsouservice</th>
                <td>{{ etsou_service.etsouservice }}</td>
            </tr>
            <tr>
                <th>Obs</th>
                <td>{{ etsou_service.obs }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ etsou_service.libelle }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('etsou_service_index') }}">back to list</a>

        <a href="{{ path('etsou_service_edit', {'id': etsou_service.id}) }}">edit</a>

        {{ include('etsou_service/_delete_form.html.twig') }}
    </div>
{% endblock %}
