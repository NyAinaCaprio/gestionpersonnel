<form method="post" action="{{ path('etsou_service_delete', {'id': etsou_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etsou_service.id) }}">
    <button class="btn btn-danger" >Delete</button>
</form>
