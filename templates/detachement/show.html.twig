{% extends 'base.html.twig' %}

{% block title %}Detachement{% endblock %}

{% block body %}
<div class="container">
    <h1>Detachement</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ detachement.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ detachement.name }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('detachement_index') }}">back to list</a>

    <a href="{{ path('detachement_edit', {'id': detachement.id}) }}">edit</a>

    {{ include('detachement/_delete_form.html.twig') }}
</div>
{% endblock %}
